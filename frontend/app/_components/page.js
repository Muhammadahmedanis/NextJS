// _compoenet is a private folder and it cannot create route but if we want to make for some reason we use %5FComponents

export default function ComponentPage() {
  return (
    <div>component page</div>
  )
}